<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SchoolReminder" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/blue_gray</item>
        <item name="colorOnPrimary">@color/ligth_gray</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/dark_gray</item>
        <item name="colorSecondaryVariant">@color/blue_gray</item>
        <item name="colorOnSecondary">@color/ligth_gray</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="background">
        <item name="android:background">@color/dark_gray</item>
    </style>
    <style name="alert_box">
        <item name="cardCornerRadius">@dimen/radius_card</item>
        <item name="android:background">@drawable/gradient_attention</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:layout_gravity">center</item>
    </style>
    <style name="alert_box_dark">
        <item name="cardCornerRadius">@dimen/radius_card</item>
        <item name="android:background">@drawable/gradient_attention_dark</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:layout_gravity">center</item>
    </style>
    <style name="box">
        <item name="cardCornerRadius">@dimen/radius_card</item>
        <item name="android:textColor">@color/white</item>
        <item name="background">@color/ligth_gray</item>
    </style>
    <style name="box_check">
        <item name="cardCornerRadius">@dimen/radius_card</item>
        <item name="android:textColor">@color/white</item>
        <item name="background">@color/blue_gray</item>
    </style>
</resources>